<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /todo/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /todo/index.html [L]
</IfModule> 